#include <stdio.h>

int main() {
    int n = 5;
    int count = 0;
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {  // nested loop
            count++;
        }
    }
    printf("O(n^2) example: Count = %d\n", count);
    return 0;
}
